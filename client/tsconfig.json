{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",       // Output dir for compiled JS files
    "lib": ["dom","esnext"],  // Use modern libraries
    "jsx": "react-jsx"        // Use React JSX
  },
  "include": ["src/**/*"],      // Specify where TypeScript source files are located
}

